# autoupdate
fir.im 自动更新SDK 帮助开发者使用一条指令更新应用，简单快速

 new FirUpdater(mActivity)
                .apiToken("xxxxxx")
                .appId("xxxxxxx")
                .forceShowDialog(true)
                .checkVersion();
